// doctest entry point for the LivePlay server unit tests.
// Built only when -DLIVEPLAY_BUILD_TESTS=ON (see server/CMakeLists.txt).
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
